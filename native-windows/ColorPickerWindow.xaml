<Window x:Class="FamidashEditor.ColorPickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Pick Background" Height="200" Width="360" WindowStartupLocation="CenterOwner">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,4">
            <TextBlock Text="R:" VerticalAlignment="Center" Width="20" />
            <Slider x:Name="RSlider" Minimum="0" Maximum="255" Width="260" />
            <TextBlock x:Name="RVal" Width="30" TextAlignment="Center" />
        </StackPanel>
        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,4">
            <TextBlock Text="G:" VerticalAlignment="Center" Width="20" />
            <Slider x:Name="GSlider" Minimum="0" Maximum="255" Width="260" />
            <TextBlock x:Name="GVal" Width="30" TextAlignment="Center" />
        </StackPanel>
        <StackPanel Orientation="Horizontal" Grid.Row="2" Margin="0,4">
            <TextBlock Text="B:" VerticalAlignment="Center" Width="20" />
            <Slider x:Name="BSlider" Minimum="0" Maximum="255" Width="260" />
            <TextBlock x:Name="BVal" Width="30" TextAlignment="Center" />
        </StackPanel>
        <StackPanel Orientation="Horizontal" Grid.Row="3" Margin="0,4">
            <TextBlock Text="A:" VerticalAlignment="Center" Width="20" />
            <Slider x:Name="ASlider" Minimum="0" Maximum="255" Width="260" />
            <TextBlock x:Name="AVal" Width="30" TextAlignment="Center" />
        </StackPanel>
        <Border Grid.Row="4" Margin="0,8" Height="48" Background="Black" x:Name="PreviewBorder" CornerRadius="4" />
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="OkButton" Content="OK" Width="80" Margin="4" />
            <Button x:Name="CancelButton" Content="Cancel" Width="80" Margin="4" />
        </StackPanel>
    </Grid>
</Window>